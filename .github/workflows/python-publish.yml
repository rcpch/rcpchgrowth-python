name: Build, verify, and publish Python package

on:
  workflow_call:
    inputs:
      ref:
        description: Exact commit to test and package
        required: true
        type: string
      version:
        description: Expected package version without the v prefix
        required: true
        type: string
